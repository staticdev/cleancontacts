---
run:
  go: "1.23"
linters:
  enable:
    - thelper
    - gofumpt
    - tparallel
    - unconvert
    - unparam
    - wastedassign
    - revive
    - forbidigo
    - tagliatelle

linters-settings:
  tagliatelle:
    case:
      use-field-name: false
      rules:
        yaml: snake
        json: snake
